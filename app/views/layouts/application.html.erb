<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "rento-copter" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <%= render '/layouts/nav' %>

    <% if flash[:notice] %>
      <div data-alert class="alert-box success radius"><%= notice %></div>
    <% end %>
    <% if flash[:alert] %>
      <div data-alert class="alert-box alert radius"><%= alert %></div>
    <% end %>

    <%= yield %>
    <%= javascript_include_tag "application" %>
    <script type="text/javascript">$(function(){ $(document).foundation(); });</script>
  </body>
</html>